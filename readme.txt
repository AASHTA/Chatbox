This is a simple Chat application that runs on console mode.

There are 3 classes for now. Server class, Client class and ChatMessage class.

To make this application more interactive, two more classes are introduced. These classes are ServerGUI class and ClientGUI class.

 
